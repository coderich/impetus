/**
 * Translator
 *
 * Responsible for taking input and translating it into an Intent
 */
export default class Translator {
  constructor(config) {
    this.config = config;
  }

  translate(input) {
    return this;
  }
}
